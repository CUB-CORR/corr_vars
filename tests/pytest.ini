[pytest]
markers =
    db: marks tests requiring a live database connection
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short

# Skip certain warnings that might come from dependencies
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
